@if(TempData["success"] != null)
{
    <script src="~/lib/jquery/dist/jquery.min.js"></script>

    <!-- Toastr notification -->
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>

    <script type="text/javascript">
        toastr.options = {
            "progressBar": true
        }

        var successList = @Html.Raw(Json.Serialize(TempData["success"]));
        successList.forEach(x => toastr.info(x));
    </script>
}

@if(TempData["error"] != null)
{
    <script src="~/lib/jquery/dist/jquery.min.js"></script>

    <!-- Toastr notification -->
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>

    <script type="text/javascript">
        toastr.options = {
            "progressBar": true
        }

        var errors = @Html.Raw(Json.Serialize(TempData["error"]));
        errors.forEach(x => toastr.error(x));
    </script>
}